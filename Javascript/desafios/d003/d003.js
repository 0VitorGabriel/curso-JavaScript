function antandsusc() {
    var number = prompt('Digite um número')
    var predecessor = Number(number) - 1
    var susccesor = Number(number) + 1
    alert(`Antes do ${number}, temos o ${predecessor}
depois do ${number}, temos o ${susccesor}`)
}